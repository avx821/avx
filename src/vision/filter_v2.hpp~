#include<math.h>
#include<stdlib.h>
#include<assert.h>
#include<iostream>
#include <ctime>

long long get_timestamp_filter();
double ema_x(double new_x, double new_time);
void set_power(double L, double R);
void reset_PID();
void thrust_x(double new_x, double new_time);
double ema_y(double new_y, double new_time);
void PID(double filter_x);
void run_filter(double X_VALUE, long long int TIME);
